# target directory. created by 'mvn clean install' and deleted by 'mvn clean'. it's all build-product that comes from the source code
target/

#output log of last time server was running
last-server-run.log
